{
  "name": "devdumpling",
  "version": "1.0.0",
  "description": "Daily GitHub Profile Generator + Web App",
  "type": "module",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview",
    "generate": "node scripts/generate.js"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22.16.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5"
  }
}